{
  "name": "absensi-backend",
  "version": "1.0.0",
  "description": "Express Backend for Absensi PWA",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "seed": "node -e \"const app = require('./index.js'); const { executeAllSeeders } = require('./seeders/index.js'); executeAllSeeders(app.pool).then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^9.0.1",
    "pg": "^8.11.5",
    "swagger-ui-express": "^5.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
